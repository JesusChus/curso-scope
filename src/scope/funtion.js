function greeting() {
    let userName = "Ana"; //Declaramos la variable
    console.log(userName); //llamamos a la variable

    if(userName === "Ana") {
        console.log (`Hello ${userName}!`); // hicimos una validacion.
    }

}
greeting(); // llamamos a la funcion